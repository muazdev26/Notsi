<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph.xml"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.muazdev.notsi.ui.home.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_upsertNoteFragment"
            app:destination="@id/upsertNoteFragment" />
    </fragment>
    <dialog
        android:id="@+id/upsertNoteFragment"
        android:name="com.muazdev.notsi.ui.upsert.UpsertNoteFragment"
        android:label="fragment_upsert_note"
        tools:layout="@layout/fragment_upsert_note" />
</navigation>