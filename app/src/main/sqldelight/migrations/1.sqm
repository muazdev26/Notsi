CREATE TABLE IF NOT EXISTS notesEntity(
    id INTEGER PRIMARY KEY NOT NULL,
    title TEXT NOT NULL,
    description TEXT NOT NULL,
    dateTime INTEGER NOT NULL DEFAULT 0,
    isDone INTEGER NOT NULL DEFAULT 0
);

ALTER TABLE notesEntity ADD COLUMN dateTime INTEGER NOT NULL DEFAULT 0;